## Log in to Claude

Your conductors build with Claude. Run **Conductor: Log In (Claude)** — it opens a terminal
running `claude setup-token`; paste the printed token back when prompted.

This mints a **portable** login that your host machines reuse — you never SSH into a box to log in.
The token is stored securely in VS Code SecretStorage and never shown in logs or messages.
