## Share your login with the fleet

Run **Conductor: Push Login to Gateway** to hand your Claude login to the gateway's encrypted
store. Every host (davinci, your Mac, …) then fetches it automatically on start — no per-host login.

You only do this once (and again whenever you re-login). To revoke it: **Conductor: Remove Login
from Gateway**.
